---
import BaseLayout from "@site/layouts/BaseLayout.astro";
import SiteHeader from "@site/components/SiteHeader.astro";
import SiteFooter from "@site/components/SiteFooter.astro";
import { pageContext } from "@site/i18n/pageContext";

const { locale, t } = pageContext();
---

<BaseLayout locale={locale} t={t} title={t.meta.privacyTitle} description={t.meta.privacyDescription} wide>
  <SiteHeader locale={locale} t={t} active="privacy" />
  <article class="prose-page">
    <h1>{t.privacy.heading}</h1>
    {t.privacy.paragraphs.map((paragraph) => (
      <p>{paragraph}</p>
    ))}
  </article>
  <SiteFooter locale={locale} t={t} />
</BaseLayout>
